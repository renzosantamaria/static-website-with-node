<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../css/<%= style %>.css">
    <title>Albums</title>
</head>
<body>
    <header>
        <div class="logo">LOGO</div>
        <nav>
            <a href="/StaticWebSite/dist/1-20.html" >First page</a>
            <a onclick="" href="/StaticWebSite/dist/327-346.html">Last page</a>
        </nav>
    </header>
    <ul>
    <% for (let i = 0; i < page.albums.length; i++) { %>
        <li class="album">
          <h1> <%= page.albums[i].Title %></h1>
          <h2 class="band">-<%= page.albums[i].Name %></h2>
        </li>
    <% } %>
    </ul>
    <footer>
        <% for(let page of pages){ %>
            <% let index = pages.indexOf(page) + 1 %> 
            <a href="/StaticWebSite/dist/<%= page.fileName %>"> <%= index %></a>
        <% } %>
    </footer>
</body>
</html>